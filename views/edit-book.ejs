<!DOCTYPE html>
<html>
<head>
	<title>Редактирование книги</title>
	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
	<style type="text/css">
		.container {
			text-align: center;
			margin-top: 30px;
		}
		form {
			width: 25%;
			margin: auto;
		}
	</style>
</head>
<body>
	<div class="container">
		<a class="btn btn-primary" href='/books/book-list'>К списку книг</a>
		<h1>Редактирование книги</h1>
		<form action="/books/edit-book" method="post">
			<input type="hidden" name="id" value="<%= book[0].id %>" />
			<div class="form-group">
				<label>Название книги</label><br>
				<input class="form-control" type="text" name="bookName" value="<%= book[0].name %>" />
			</div>
			<div class="form-group">
				<label>Автор</label><br>
				<input class="form-control" type="text" name="authorId" list="type-list" value="<%= book[0].authorId %>" autocomplete="off" />
				<datalist id="type-list">
					<% for(let i = 0; i < authors.length; i++) { %>
					<option value="<%= authors[i].id %>"><%= authors[i].authorSurname %> <%= authors[i].authorName %></option>
					<% } %>
				}
			</datalist>
		</div>
		<input class="btn btn-primary" type="submit" value="Изменить" />
	</form>
</div>
</body>
</html>